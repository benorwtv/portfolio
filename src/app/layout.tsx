import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "Ruben Monteiro | AI Specialist & Software Developer",
  description: "Portfolio of Ruben Monteiro, an AI Specialist and Software Developer based in Esmoriz, Portugal, delivering AI-powered solutions and custom software.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans`}>{children}</body>
    </html>
  );
}
